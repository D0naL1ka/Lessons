Масиви:

int arr[] = {1, 2, 2, 3};
	const int arr_size = sizeof(arr) / sizeof(arr[0]);

	cout << "array 1" << endl;
	for (int i = 0; i < arr_size; i = i+1) {
		cout << arr[i] << " ";
	}	

	for (int i : arr) {
		cout << i << "";
	}

	int b_arr[arr_size]{};
	for (int i = 0; i < arr_size; i++) {
		b_arr[i] = arr[i];
	}
	cout << "array 2" << endl;
	for (int i : b_arr) {
		cout << i << "\n";
	}

	b_arr[0] = 5;
	cout << "array 2" << endl;
	for (int i : b_arr) {
		cout << i << "\n";
	}
	cout << "array 1" << endl;
	for (int i : arr) {
		cout << i << "";
	}